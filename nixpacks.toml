[phases.setup]
nixPkgs = ["python311", "ffmpeg-full", "dejavu_fonts"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT"