[build]
  command = "cd frontend && npm install --legacy-peer-deps && npm run build"
  publish = "frontend/build"

[build.environment]
  REACT_APP_BACKEND_URL = "https://listworks-pro-v2-production.up.railway.app"